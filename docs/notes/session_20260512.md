# 2026-05-12 세션 기록

## 합의된 추천 작업 순서 (사용자 + Codex)

1. ✅ 거래 신고 시스템 (ROADMAP 출시 블로커)
2. ⏸️ 첫 실행 onboarding (거래 탭 Coach Mark는 검색 풀스크린 modal로 흡수, 전체 onboarding은 미진행)
3. ⏸️ 호가 클릭 → 채팅 자동 생성
4. ⏸️ 인증 통일 (Kakao 단일)
5. ⏸️ 체결 처리 + PEER_TRADE 이력

## 오늘 완료한 작업

### 백엔드
- **거래 신고 시스템 (Reports)**
  - `back/.../domain/report/Report.java` — 엔티티 (PENDING/REVIEWED/RESOLVED/DISMISSED)
  - `ReportRepository.java`, `ReportController.java` — `POST /api/reports`, `GET /api/reports/me`
  - targetType 4종(TRADE/USER/BUY_ORDER/CHAT), reason 6종 화이트리스트
  - 동일 사용자 동일 대상 PENDING 중복 제한
  - `db/schema.sql`에 reports 테이블 추가 + 인덱스 3개
  - `SecurityConfig`에 `/api/reports/**` permitAll 추가
- **BGS → BRG 등급사 표기 통일**
  - `BuyOrder.java` 주석, `price_ebay.py` 키워드, `crawl_bunjang.py` 정규화, `asset_screen.dart`(2곳), `card_detail_screen.dart`
  - 외부 데이터는 BGS도 인식하되 결과는 BRG로 정규화

### 프론트
- **푸터 자산 → 챗 교체**
  - `main_shell.dart`: 자산 탭 → 챗 탭, unread badge 챗으로 이동, didUpdateWidget으로 라우트 변경마다 reload
  - `home_screen.dart`: "자산 보기" → "전체 보기" + `GestureDetector` 진입
  - `profile_screen.dart`: "내 매수 주문" 메뉴 추가 (`/api/buy-orders/me` 카운트), 채팅 메뉴 제거
  - `app_router.dart`: `/chat-list`를 ShellRoute 안으로 이동 (푸터 유지)
  - `asset_screen.dart`: `initialTabIndex` 파라미터 + `didUpdateWidget` (`/assets?tab=buy`)
- **푸터 FAB 시트 제거 → 즉시 스캔**
  - `main_shell.dart`: `_showFabOptions()` 삭제, FAB 탭 = 바로 `/scanner` push
- **카드 상세 "내 자산" CTA 단일화 + expectedCardId 가드**
  - `card_detail_screen.dart`: "직접 추가" CTA 제거, "스캔으로 추가"만, `/scanner?expectedCardId=` 전달
  - `_lookupOwnedAsset()` 추가: 검색·거래 탭 진입 시 사용자 자산 자동 매칭
- **스캐너 expectedCardId 가드 + Mismatch overlay**
  - `scanner_screen.dart`: `expectedCardId` 받으면 cardId 비일치 시 등록 차단, "다른 카드입니다" 거부 화면 (재스캔만, 우회 불가)
  - 라우터 query param 처리
- **거래 탭 풀스크린 검색 modal**
  - `trade_search_screen.dart` 신규 — autofocus TextField + 자동완성 + 최근 검색 + 카드 결과
  - `trade_list_screen.dart`: "판매하기" 제거, 검색 아이콘 → `Navigator.push(fullscreenDialog)`
  - 카드명 자동완성: `/api/cards/search` 호출 + suffix(EX/V/VMAX/GX/BREAK) 정규화로 unique 묶음
  - suggestion 탭 → `/api/cards/market` 본격 검색
  - 최근 검색: `flutter_secure_storage`에 최대 10개, chip + 개별 삭제 + 전체 삭제
  - 빈 상태: 상단 정렬 (키보드에 안 가림) + "판매 / 구매 호가 등록" 안내
- **카드 상세 multi-step Coach Mark (당분간 보류 처리)**
  - 거래 탭 1단계 Coach Mark는 풀스크린 modal로 대체되며 폐기
- **K/S 등급 프론트 hide** — 검색 rarities에서 K, S 제거 (DB는 유지)
- **버그 픽스**
  - `chat_room_screen.dart`: STOMP onConnect/subscribe/onDisconnect/onWebSocketError + `_loadMessages` + `_scrollToBottom` 모두 `mounted` 가드
  - `trade_search`: request token으로 stale 응답 무시 (debounce 후 빠른 변경 race)
  - `trade_search.dart` Scaffold `resizeToAvoidBottomInset: false` — 키보드 위 검정 띠 제거
  - `main_shell` 키보드 위 FAB/푸터 검정 영역 fix
- **iOS 키보드 cold-start lag 해결**
  - 첫 진입 검색 transition lag 원인 = iOS keyboard subsystem cold-start (200~500ms)
  - 풀스크린 modal 진입 애니메이션이 cold-start 시간 자연스럽게 가림
  - 시도했던 다른 해결책(warmup, AnimatedSwitcher morph)은 race condition 발생해 폐기
- **거래 검색 백엔드 응답 형식 fix**
  - `getList` 폐기, `ApiClient.get` + `res['data']` 추출로 변경 (ReturnData wrapper)
- **검색 안내 문구 자연스럽게** — "매도(판매) / 매수(삽니다)" → "판매 / 구매"
- **홈 캐러셀 미세 조정**
  - viewportFraction 0.72 → 0.6 (현재카드↔다음카드 사이 여백 ↓)
  - height 0.40 → 0.46 (max 460, 카드 더 크게)

### 작업 중 폐기/원복된 것
- **3D 회전문 캐러셀 (Cylinder Carousel)** — 사용자가 의도한 원통 바깥면 회전 구조 구현 시도했으나 회전 방향(안쪽 vs 바깥쪽), 뒷면 표시 임계, peek layer 등 여러 차례 어긋남. 최종적으로 `git checkout HEAD --`로 원복하고 PageView 캐러셀 유지 결정. **다음 세션에 재도전.**

## Codex와의 합의/논쟁
- **카드 상세 "직접 추가" 제거**: Codex 처음 반대 → 사용자 데이터 정합성 우선 논거로 설득, 합의 후 진행.
- **푸터 자산 → 챗 교체**: Codex 부분 반대 (포트폴리오 정체성) → 사용자 "거래 커뮤니티 형성에 챗 first-class 필수" 논거 + C2C 마켓 레퍼런스(당근/번개/StockX/GOAT 모두 챗 푸터)로 합의.
- **3D 캐러셀 회전 방향**: Codex가 "안쪽 회전(A)"이 reference 시각 결과와 일치한다고 판단했으나, 사용자의 김밥 비유와 명시적 의도("바깥쪽")가 절대 기준. 다음 세션 시작 시 이 컨센서스 유지하고 진행할 것.

## 검증
- `flutter analyze`: error 0 (모두 info 또는 deprecated_member_use 기존 경고)
- `./gradlew compileJava`: BUILD SUCCESSFUL
- 백/그레이딩/스캐너 health 통과 (restart.sh 적용 완료)

## 다음 세션 작업 계획

### 1순위: 첫 실행 onboarding
- 첫 진입 시 홈 화면에서 "카드 추가하기" CTA → 스캔 진입
- 거래 탭 Coach Mark는 풀스크린 modal 진입 자체가 사용성 안내 역할이라 별도 X
- 카드 상세 step2 Coach Mark는 일단 보류 결정

### 2순위: 호가 클릭 → 채팅 자동 생성
- 카드 상세의 매수/매도 호가 행을 누르면 해당 사용자와 자동 chat room 생성
- BuyOrder 작성자 ↔ 카드 보유자 매칭 흐름

### 3순위: 인증 통일 (Kakao 단일)
- 현재 일반 로그인 + Kakao 혼재 → Kakao만으로

### 4순위: 체결 처리 + PEER_TRADE 이력
- 채팅에서 거래 완료 시 PEER_TRADE 시세 기록
- 시세 통계에 반영

### 배포 전 P0 (메모리에 기록됨)
- SecurityConfig 정리, JWT secret 환경 변수, CORS allowed origins

## 보류/폐기된 시도
- **3D 회전문 캐러셀** — 이번 세션 다섯 차례 시도했으나 회전 방향(안쪽 vs 바깥쪽), 뒷면 peek 표현, PageView vs Stack 선택에서 사용자 의도와 계속 어긋남. `git checkout HEAD --`로 원복. 다음 세션 우선순위에서 제외하기로 사용자 결정.

# 시세 복구·영구화 종결 (2026-06-17)

> 6/15~6/16 KO 시세 붕괴 사고의 **근본 교정 + 재발방지 영구화**. 전부 prod LIVE.
> 상세 디버깅 이력 = memory `project_price_recovery_20260616` / `docs/PRICE_RECOVERY_HANDOFF_20260616.md`.
> 재캘리 절차 = `docs/PRICE_RECALIBRATION_RUNBOOK.md`.

## TL;DR
깨졌던 것(노화 절벽·source flip 폭등/폭락·급변동 오표시·계수차트 cliff) **전부 고쳐서 배포**.
시세는 매일 JP/EN raw로 **라이브**, 그 위에 곱하는 **"한국 배율(계수)"만 6/14 값으로 freeze**해서 노화-절벽을 차단.

## 근본 원인 (2개)
1. **6/15 base sync 부분실패** → prevSource 증발 → 6/16 히스테리시스 임계(2.2/1.8/2.0) 붕괴 → source flip(JP↔EN) → 폭등 26 + 폭락 59.
2. **DAANGN obs 노화** — 비율/계수를 매 실행 90일 롤링 윈도우로 라이브 재집계 → 3/16 대량수집이 6/16에 만료되며 풀 붕괴 → 절벽. **recurring · self-heal 안 됨 · 코드/내수정 무관(달력 이벤트).**

## 한 일 (전부 prod LIVE, 2026-06-17)
| # | 내용 | 메커니즘 / 위치 |
|---|---|---|
| 1 | 6/15 백필 (3,455장) | 6/14 carry, id `ko_bf0615_%` |
| 2 | 6/16 source-flip 교정 78장 | `ko_estimation_audit` EN→JP (오늘밤 base self-heal) |
| 3 | **frozen-v6 배포** | `v6_apply.py` 풀비율(RRH/GHIT/TIERR/RTR/PRTR) + **카드별 DAANGN(`ko_vals` 226장)** 전부 6/14 상수 주입 → 노화절벽 영구차단. 피카츄 AR 205/172 앵커 71,520 추가 |
| 4 | **6/16 reconcile** | frozen-v6 real apply 1,047장(down 1,025/up 22) → 6/14≈6/15≈6/16≈6/17 동일기준 = 차트 매끈 + 6/16 잔여인플레 교정 |
| 5 | **급변동 보드 수정** | `CardRepository` 급상승/급하락 frac을 JP/EN raw 투영 → **실제 KO 7일변동**(kop CTE·`::numeric`). 차트와 일치 |
| 6 | **글로벌계수 freeze** | `GlobalPriceService.FROZEN_GLOBAL_COEFFICIENT=0.478` (재계산은 로그 관측만) + 계수추세 차트 cliff 데이터 평탄화(6/15~17→4780). 차트 06-14~06-17 flat 확인 |

## 불변식
- `KO = 해외 raw(라이브) × 배율(frozen 6/14)`
- **down-only 우선**, up은 게이트(50) + `MANUAL_ANCHOR` 만
- **`price_snapshots` 직접 UPDATE 금지**(밤 cron이 덮음) → `v6_apply.py` / 앵커로만 반영
- 앵커 = 사용자 확정 하드값(JP 드리프트만 추종), 재캘리해도 유지

## 롤백 포인트 (prod)
- frozen-v6 이전: `/opt/pokefolio/scripts/v6_apply.py.bak_pre_freeze_20260617`
- 6/16 reconcile 이전: 테이블 `ko_v6enbr_rollback_20260616` (어젯밤 cron 백업 = `..._20260616_cron`)
- 계수차트: `coef_hist_bak_20260617`
- 6/16 price/audit: `ko_fix0616_bak` / `audit_fix0616_bak`
- 글로벌계수 코드: 이전 상수로 되돌려 BE 재빌드

## 오늘밤 자동검증
```
23:15 계수재계산(frozen 0.478) → 23:45 base sync → 23:52 frozen-v6(노화無) → 23:55 sanity
```
자정 후 자동 모니터가 6/17 검증(폭등>1.8x / 폭락<0.55x = 0, 메가망나뇽 19,640, 피카츄 71,520, v6 로그 ABORT 없음) → 결과 보고.

## 남은 것 (백로그, 안 급함)
- **7 ANCHOR_NEEDED** (히드런EX·볼트로스EX·윤진·딩루ex·음번ex·이벨타르ex·N의각오): v6 down-only가 1차로 잡음, 오늘밤 자동확인 포함, 필요시 앵커
- **월1회 재캘리** (frozen=정적 → stale 방지): `docs/PRICE_RECALIBRATION_RUNBOOK.md`. 트리거(캘린더 vs 이메일 cron) 미정
- **P3 dual-basis** (EN/JP 2~4배 격차 = flip의 토양) — 깊은 숙제
- **P2 등급계수 정화** (AR/HR=DAANGN, CHR 수동)
- (cosmetic) 계수차트 라벨 "새벽 4시30분" = 실제 재계산 23:15 (frontend 표기 stale)

# Handoff — 2026-05-31 새벽 (Phase 1 종결 + Phase 2 진입)

> 작성: 2026-05-31 03:00 KST. 어제(5-30) ~ 오늘(5-31) 새벽까지 약 30시간 cycle 결과.

---

## 0. 종합 — 한 줄

```
Phase 1 (도감) 완전 종결 ✅
시세 동기화 사고 복구 + 재발 방지 ✅
Phase 2 (AI 그레이딩) spec 확정 — 내일 개발 시작
```

3 cycle commit + 시세 사고 디버깅 + 데이터 정리 + IPA 빌드 + Phase 2 spec.

---

## 1. Commit History

| Commit | 내용 |
|--------|------|
| `ee1feab0` | Cycle 1 — 닌자스피너 master + S3 이미지 + 가격 backfill |
| `6120a3d3` | Cycle 2 — dex_hit_card_ids override + 5-6장 표시 |
| `a7c588b0` | Cycle 3 — 위험 endpoint 가드 + cron 영구화 |

---

## 2. Cycle 1 — 닌자스피너 데이터 보강 (ee1feab0)

- **Master**: 27장 INSERT (SAR 6 + AR 11 + SR 10). 095/109/111 한국명 보류.
- **S3 이미지**: 27장 mirror (`cards/v1/jp/{cardId}.png`).
- **SCRYDEX_JP raw 14일**: 392건 backfill.
- **KO_ESTIMATED 14일**: 391건 history backfill (`/api/internal/admin/backfill-ko-history` endpoint 신규).
- **재현 SQL**: `back/sql/ninja_spinner_kr_catalog_backfill_20260530.sql` (idempotent NOT EXISTS).

산식 검증: KO = `SCRYDEX_JP_KRW × jp_rarity_coef` (운영 산식, GlobalPriceService line 2321).

---

## 3. Cycle 2 — 도감 hits override (6120a3d3)

- **Schema**: `products.dex_hit_card_ids text` (CSV, NULL = 자동 fallback).
- **Backend**: DexService 분기 — byId map + CSV split + cap 6 + 자동 fallback.
- **Flutter**: `dex_detail_screen.dart` clamp(0, 6).
- **데이터**: 40 product UPDATE (자동 v3 33 + 수동 보정 7).
  - 컬렉션형 6장: VSTAR 유니버스 / 테라스탈 페스타 / VMAX 클라이맥스
  - 5장: 포켓몬 카드 151
  - 수동: 니힐제로 hero 중복 강제 / 스타트덱 100 / MEGA 드림 ex 순서
- **Codex review**: GO + 5 조건 충족.

---

## 4. Cycle 3 — 위험 endpoint 가드 + cron 영구화 (a7c588b0)

### 위험 endpoint 차단 (8개 POST)
- `DangerousAdminFilter` (Spring `OncePerRequestFilter`)
- `app.admin.dangerous-price-actions.enabled=false` (기본)
- 차단 대상:
  - `/api/internal/admin/refresh-ko-estimates`
  - `/api/internal/admin/backfill-ko-history`
  - `/api/prices/admin/refresh-ko-estimates`
  - `/api/prices/admin/backfill-ko-history`
  - `/api/prices/admin/refresh-ko-live`
  - `/api/prices/admin/market-adjustment`
  - `/api/prices/admin/clear-card-mappings`
  - `/api/prices/admin/recalculate-en-jp-ratios`

### Admin web 위험 버튼 차단
- `Price.jsx`의 "KO 예상가 재계산" → 차단 안내 UI
- "적용 및 재계산" (market-adjustment) → 차단 안내

### Cron 영구화 (`back/cron/`)
- `hold_outliers_daily.sh` (v2 — 동적 count + 두빅굴/차곡차곡 추가)
- `v6_apply_daily.sh`
- `sanity_cap_extreme_daily.sh`
- `README.md` (운영 흐름 + 변경 이력)

---

## 5. 시세 동기화 사고 — 원인 + 복구

### 사고 원인
1. 우리 04:05 `refresh-ko-estimates` 수동 호출 → `v6_apply` 보정 우회
2. 22:19 우리 `v6_apply` 수동 실행 → `ko_v6enbr_rollback_20260530` backup table 생성
3. 23:45 Spring `@Scheduled refreshKoEstimates()` → today KO 전체 재생성 (정상 흐름)
4. **23:52 v6_apply cron ABORT** (우리 backup 충돌) ← 진짜 사고
5. 23:55 sanity_cap 정상 → chase 카드 시세 망가짐 노출

### 복구
- 00:18 `DROP TABLE` + `v6_apply` 재실행 → 1,783 카드 복구.
- 두빅굴 EX RR / 차곡차곡 GX SR hold UPDATE (scrydex eBay scrape mismatch).
- 모야모 SAR 14일 EN RAW 비율 자연 평탄화 (185K~200K).
- 비파리온 V (swsh2a-73) hide + price_snapshots 67 row DELETE.

### Backup 보존
- `ko_moyamo_sar_backup_20260531` (원본 22K/33K/218K)
- `ko_moyamo_sar_200k_backup_20260531` (200K 일괄)
- 현재 = EN RAW 비율 자연 변동 (185K~200K)

---

## 6. Phase 3 Pending — 출시 후 또는 별 cycle

| Task | 내용 |
|------|------|
| #86  | 폭염워커 비파리온 V 가격 변동 조사 |
| #94  | v6_apply backup 정책 fix (수동 실행 시 cron 충돌 0) |
| #96  | 23:45 refresh 동작 깊이 분석 (이미 도출됨) |
| #98  | SCRYDEX sanity check (통화/native variance 자동 hold) |
| #100 | UI anomaly 가드 (전일대비 100%+ 숨김) |
| #102 | ★ **최우선** v6 HIT carry on/off 일관성 fix |
| -    | 비파리온 V swsh2a-25 (RR) 매핑 검증 |
| -    | "비파리온" → "샤미드" 옛 4장 한국명 통일 |
| -    | 닌자스피너 095/109/111 한국명 +3장 (39장 완성) |
| -    | admin web Vite rebuild (Price.jsx 차단 UI 시각 반영) |

---

## 7. Phase 1 도감 — 완전 종결 ✅

TestFlight 시각 검증 통과:
- 닌자스피너 36장 카드 이미지 정상
- 힛카드 4장 (MUR 메가개굴닌자 / SAR 메가개굴닌자 / SAR 치라치노 / SAR 메가드래캄)
- 비파리온 V 안 보임 ✅
- 시세 6 chase 카드 = 어제 = 오늘 정상

---

## 8. Phase 2 AI 그레이딩 — 다음 (`docs/GRADING_PHASE_2_MVP.md` 참조)

### 사용자 명시 새 plan
1. **카메라 + 우리만의 frame overlay** (Legends 그리드 따라하기 X, 자체 센터링 reference)
2. **앞면/뒷면 2장** (8 코너 분리 X, 자동 ROI)
3. **채도/명암 변환 기반 결함 visibility** (홀로/패턴 카드에 효과)
4. **결함 위치 시각화** (점수 + bbox overlay + "여기 ↘ 화이트닝")
5. **자체 등급 S+/S/S-/A+/A/A-/B+/B/C** (PSA 매핑 0)
6. **0.1 단위 점수** + 감점 사유 명확
7. **weighted_raw + min_metric + major defect = 3중 cap** (weakest link rule)
8. **2일 MVP** (Day 1 백엔드 + Day 2 Flutter)

### 정책
- Tier 1 (commit 316ff049) 정책 유지 + 강화
- 외부 등급사 이름은 disclaimer 만
- ML 모델 X (출시 후)
- 데이터셋 수집 X (출시 후)

---

## 9. 출시 D-7 잔여 phase

| Phase | 내용 | 소요 |
|-------|------|------|
| Phase 1 도감 | ✅ 종결 | - |
| Phase 2 AI 그레이딩 | 카메라 frame + 채도/명암 + 시각화 (2일 MVP) | 2일 |
| Phase 3 디버깅 | UI anomaly 가드 / v6 carry 일관성 / 한국명 등 | 2-3일 |
| Phase 4 UI/UX polish | 시세 그래프 / 푸시 / 호가창 검증 | 1일 |
| **Phase 5 보안** | 🚨 SecurityConfig + JWT + CORS ([[project-must-before-deploy]]) | 0.5일 |

---

## 10. 내일 시작 path

1. **컨디션 회복** (지금 새벽 3시 + 30시간 누적)
2. **5-31 cron 검증** (오늘 밤 23:50/52/55 흐름 + 모야모 재발 여부)
3. **Phase 2 Day 1** — `analyzer.py` 확장 (DefectRegion + DeductionReason + 9단계 grade)
4. **Phase 2 Day 2** — Flutter (`grading_capture_screen.dart` 카메라 + `grading_result_screen.dart` 결함 overlay)
5. **TestFlight 재빌드** → 시각 검증 → Phase 3 진입

---

## Memory 인덱스 (참조)

- `[[project-ninja-spinner-catalog]]` — 닌자스피너 27장 cycle
- `[[project-chase-pricing-model-status]]` — 시세 freeze 정책
- `[[feedback-grading-companies]]` — PSA/BRG 만, CGC/BGS 금지
- `[[project-must-before-deploy]]` — 출시 전 보안 패치 3종
- `[[feedback-release-timing-user-owns]]` — 출시 일정 사용자 단독

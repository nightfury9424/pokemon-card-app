# PokeFolio v1.0.1 — 첫 업데이트 (버그픽스 + 거래 발매판 분리)

> **상태**: 🟡 심사 중 (2026-06-11 제출). 빌드 `202606111444`, git 태그 `v1.0.1-rc` (`8cf1ef04`).
> 승인 시 `main`을 `v1.0.1-rc`로 ff + `v1.0.1` 태그.
> 범위 = `v1.0`(`d579a01a`) 이후 변경.

---

## 한 것 (커밋 기준)

### 🐛 버그픽스 4종 — `545534ad`
- 검색 무한로딩 해결
- 상세 첫 진입 시 "이미지 없음" 깜빡임 해결
- 정지 → 로그아웃 흐름 fix
- 점검 페이지(maintenance 화면) 추가

### 🌐 거래 발매판(KO/JP/EN) 분리 — `97fe32f1` · `50409190` · `13610c42`
- 발매판별 호가 분리 + 발매판별 시세 KRW
- 예상가 자동입력 (발매판 선택 시)
- 홈 발매판 라벨 표시 fix
- 매수폼 발매판 선택 picker (#4·#5)
- 판매글 발매판 표시
- 스캐너 등록 시트 카드언어 탭이 발매판에 적응

### 🗄 마이그레이션 안전 — `9033afa4`
- `language` 컬럼 `DEFAULT 'KO'` — 배포 윈도우에 구버전 코드가 NULL INSERT 하는 것 방지
  - ※ **이 사례가 v1.0.2 강제 업데이트 게이트의 정당화 근거** (구버전 클라 ↔ 새 스키마 충돌)

### 📱 iOS 설정 — `741ffd98` · `8cf1ef04`
- `CFBundleDevelopmentRegion=ko` + `CFBundleLocalizations[ko]`
- **iPhone 전용** (`TARGETED_DEVICE_FAMILY=1`, iPad 제외 — 무한스피너 반려 회피)
  - ※ working tree에만 떠 있던 변경을 `8cf1ef04`로 커밋해 **제출 바이너리 = git 소스 일치**

### 🛡 관리자 (admin web, iOS 앱 외) — `845d9353`
- 모더레이션 워크벤치 1차 — 신고 SLA 타이머·지표바·마감순 정렬

---

## 배포 메모
- **BE prod 배포 완료** (`feat/trade-active-counterparty` @ `9033afa4`, 발매판 BE 포함). 실기기 검증 ✓
- 배포 메커니즘: Flyway 안 씀 + `ddl-auto=validate` → **수동 마이그 먼저 → 코드 배포**
- iOS: 빌드 `202606111444` Transporter 업로드 → ASC 제출
